<h1>BURGER TIME!</h1>

<h2>Gotta eat these burgers still..</h2>

<ul>
  {{#each burgers}}
    {{#unless this.devoured}}
      
    {{/unless}}
  {{/each}}
</ul>

<h2>CRUSHED 'EM</h2>

<ul>
  {{#each burgers}}
    {{#if this.devoured}}
      
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit" name="addIt">Add Burger</button>
</form>
